name: Play with GA

on:
  workflow_dispatch:
    inputs:
      env:
        required: true
        default: dev
        type: choice
        options:
          - dev
          - prod

jobs:
  simple-job:
    runs-on: ubuntu-latest
    
